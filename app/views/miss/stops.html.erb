<h2><%= page_title %></h2>

<div class="lines paginated_content">
<table class="table table-hover table-striped">
  <thead>
    <tr>
      <th>Nom</th>
      <th>Identifiant</th>
      <th>Type</th>
      <th>SIM</th>
      <th>Code INSEE</th>
      <th>Mode de transport</th>
      <th>Lat</th>
      <th>Lon</th>
      <th>Action</th>
    </tr>
  </thead>

  <tbody>
    <% @stops.each do |stop| %>
      <tr>
        <td><%= link_to stop.name, stop_path(stop.id) %></td>
        <td><%= stop.code %></td>
        <td><%= stop.quay_type %></td>
        <td><% if stop.mis %> <%= link_to stop.mis.name, miss_path(stop.mis_id) %><% end %></td>
        <td><%= stop.administrative_code %></td>
        <td><%= stop.transport_mode %></td>
        <td><%= stop.lat %></td>
        <td><%= stop.lon %></td>
	<td><%= link_to 'Visualiser', "stops/#{stop.id}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<div class="pagination">
  <%= will_paginate @stops, :container => false, renderer: BootstrapPagination::Rails %>
</div>

<%= link_to 'Back', miss_path(@mis) %>
