<!DOCTYPE html>
<html>
<head>
  <title>ApiiSimBackoffice</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body role="document">
  <div id="header" class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to t("application.root_name"), root_path, :class => "navbar-brand" %>
      </div>
      <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav">
        </ul>  
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t("application.data") %><b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "#{t('application.data_sims')} <span class='badge pull-right'>#{Mis.all.size}</span>".html_safe, miss_index_path %></li>
              <li><%= link_to "#{t('application.data_stops')} <span class='badge pull-right'>#{Stop.all.size}</span>".html_safe, stops_path %></li>
            </ul>
          </li>
          <li><a href="#"><%= t("application.import") %></a></li>
        </ul>
      </div>
    </div>
  </div>
  
  <div class="container  <%= controller_name %> <%= action_name %>">
    <%= render partial: "shared/flash_messages", flash: flash %>
    <%= render_breadcrumbs :separator => " > " %>
    
    <%= yield %>
    
    <div class='footer row'>
      <div class='span12'>
        <hr>
        <p><%= t("welcome.title") %></p>
        <p><%= t("welcome.version") %></p>
      </div>
    </div>    
  </div>
</body>
</html>
