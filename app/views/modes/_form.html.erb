<%= form_for(@mode) do |f| %>
  <% if @mode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mode.errors.count, "error") %> prohibited this mode from being saved:</h2>

      <ul>
      <% @mode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mode_code %><br>
    <%= f.text_field :mode_code %>
  </div>
  <div class="field">
    <%= f.label :mode_description %><br>
    <%= f.text_area :mode_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
